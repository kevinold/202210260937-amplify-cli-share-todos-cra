type TodoGroup
  @model
  @auth(rules: [{ allow: owner }])
{ 
  id: ID!
  name: String
}
type Todo
  @model
  @auth(rules: [{ allow: owner }, { allow: groups, groupsField: "group"}]) 
{ 
  id: ID!
  content: String
  group: String
}
